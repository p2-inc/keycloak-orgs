import { apiConfig, apiRealm } from './helpers';
import { AttributesApi, EventsApi, EventRepresentation, RealmAttributeRepresentation, WebhookRepresentation } from '@p2-inc/js-sdk';

const attributesApi = new AttributesApi(apiConfig);
const eventsApi = new EventsApi(apiConfig);                                                          

export const Realms = {


}
